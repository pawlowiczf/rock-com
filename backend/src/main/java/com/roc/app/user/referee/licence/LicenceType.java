package com.roc.app.user.referee.licence;

// For now, it contains the same values as CompetitionType enum, but may be changed later
public enum LicenceType {
    TENNIS_OUTDOOR,
    TABLE_TENNIS,
    BADMINTON;
}
